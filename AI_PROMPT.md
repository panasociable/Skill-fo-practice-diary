# Универсальный промпт для оформления Дневника практики ПГТУ

Скопируй текст ниже в любой ИИ (ChatGPT, Gemini, DeepSeek и т.п.), который умеет
выполнять код, и **прикрепи к сообщению два файла**: `fill_diary.py` и
`template.docx`. В конце допиши свои данные в свободной форме.

---

Ты помогаешь студенту оформить «Дневник практики» ПГТУ. Я прикрепил скрипт
`fill_diary.py` и шаблон `template.docx`.

Сделай по шагам:

1. Прочитай мой рассказ ниже и вытащи поля для JSON. Обязательные: `fio`,
   `kafedra`, `spec`, `kurs`, `gruppa`, `mesto`, `start_date`, `end_date`
   (формат `ГГГГ-ММ-ДД`), `zadanie`. Необязательные: `vid` (по умолчанию
   «производственная»), `tip`, `forma` (по умолчанию «очная»), `prikaz_n`,
   `prikaz_date`, `dog_n`, `dog_date`, `instr_vuz`, `instr_org`.
2. Если каких-то обязательных полей не хватает — задай мне один короткий
   уточняющий вопрос списком, не растягивай на много сообщений.
3. Когда данные собраны, установи `python-docx`, сохрани JSON в `data.json` и
   запусти: `python fill_diary.py data.json "Дневник_Фамилия.docx"` (если
   доступен только `python3`, используй его).
4. План работ из 8 пунктов и даты по ним скрипт считает сам — их вводить не нужно.
5. Проверь, что в готовом документе не осталось текста вида `{{...}}`, и пришли
   мне файл `.docx`.

Мои данные:
> (напиши здесь о себе и практике в свободной форме)
